name: money_buddy
description: "Money Buddy - Financial chatbot with gamification"

publish_to: 'none'

version: 1.0.2+3

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  http: ^1.1.0
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  flutter_tts: ^4.2.3
  speech_to_text: ^6.3.0
  image_gallery_saver: ^2.0.2
  share_plus: ^7.2.2
  uuid: ^4.0.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
